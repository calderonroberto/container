<div class="tutorial-text">
  <p>This Display's Name: <%= @display.name %> </p>
  <p>This Display's public URL: <%= link_to @publicurl, @publicurl, data: { confirm: "Leave Admin Page?" } %>  </p>
  <p>This Display broadcasts checkin events at: <%= link_to "#{@setup.thingbroker_url}/things/checkin#{@display.unique_id}/events", "#{@setup.thingbroker_url}/things/checkin#{@display.unique_id}/events", data: { confirm: "Leave Admin Page? This is a long-pull, wait 30 secs for response." } %> (<%= "checkin#{@display.unique_id}" %>)</p>
</div>

<%= will_paginate @apps %> 
<div class="span11">
  <ul class="apps">
    <%= render @apps %>
  </ul>
</ul>
<%= will_paginate @apps %>
