<% @apps.each do |app| %>
<%= link_to image_tag(app.thumbnail_url, :border => 0, :width => 60, :height => 60), {:action => 'show', :controller => 'mobile', :id => app.id, :class => 'appthumbnail'}, {id: app.id} %>
<% end %>
