<!DOCTYPE html>
<html lang="en">
<head>
  <title>MAGIC Container</title>

  <!-- We need JS here to run document.ready scripts for mobile --> 
  <%= javascript_include_tag "application" %>

  <!-- styles --> 
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <!-- responsive css for mobile-->
  <%= stylesheet_link_tag "bootstrap-responsive.min", :media => "all" %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no"> 
  <%= csrf_meta_tags %>

</head>
<body>

<%= render 'layouts/mobile_header' %>

<div class="container">
<% flash.each do |key, value| %>
  <%= content_tag(:div, value, class: "alert alert-#{key}") %>
<% end %> 
<%= yield %>
</div>

<!-- Place js at end of file so that page loads faster --> 
<%= javascript_include_tag "mobile" %>
</body>
</html>
